apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: todo-cluster
  region: eu-west-2

managedNodeGroups:
  - name: spot-ng-1
    instanceTypes: ["t3.medium", "t3a.medium"]
    spot: true
    desiredCapacity: 1
    volumeSize: 20
